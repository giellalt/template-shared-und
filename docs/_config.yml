theme: jekyll-theme-giellalt
title: Shared __UNDEFINED__ Names
description: Shared __UNDEFINED__ lexical and other resources for use across GiellaLT language models

defaults:
  - scope: { path: "" }
    values:
      layout: default

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable: true
